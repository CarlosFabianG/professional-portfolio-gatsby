import Typography from 'typography'

const typography = new Typography({
    baseFontSize: 16,
    baseLineHeight: 1,
    headerFontFamily: [`Ibarra Real Nova`],
    bodyFontFamily: ['Public Sans']
})

export default typography